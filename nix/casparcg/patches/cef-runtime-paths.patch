diff --git a/CMakeModules/Bootstrap_Linux.cmake b/CMakeModules/Bootstrap_Linux.cmake
index 2dc373e..446762c 100644
--- a/CMakeModules/Bootstrap_Linux.cmake
+++ b/CMakeModules/Bootstrap_Linux.cmake
@@ -13,6 +13,9 @@ endif()
-
+
 set(ENABLE_HTML ON CACHE BOOL "Enable CEF and HTML producer")
 set(USE_STATIC_BOOST OFF CACHE BOOL "Use shared library version of Boost")
 set(USE_SYSTEM_CEF ON CACHE BOOL "Use the version of cef from your OS (only tested with Ubuntu)")
+set(CASPARCG_CEF_INCLUDE_PATH "/usr/include/casparcg-cef-142" CACHE PATH "CEF include root")
+set(CASPARCG_CEF_LIBRARY_PATH "/usr/lib/casparcg-cef-142" CACHE PATH "CEF library directory")
+set(CASPARCG_CEF_RESOURCE_PATH "${CASPARCG_CEF_LIBRARY_PATH}" CACHE PATH "CEF resource directory")
 set(CASPARCG_BINARY_NAME "casparcg" CACHE STRING "Custom name of the binary to build (this disables some install files)")
 set(ENABLE_AVX2 ON CACHE BOOL "Enable the AVX2 instruction set (requires a CPU that supports it)")
@@ -49,14 +52,13 @@ endif()
 if (ENABLE_HTML)
     if (USE_SYSTEM_CEF)
-        set(CEF_LIB_PATH "/usr/lib/casparcg-cef-142")
-
         add_library(CEF::CEF INTERFACE IMPORTED)
         target_include_directories(CEF::CEF INTERFACE
-            "/usr/include/casparcg-cef-142"
+            "${CASPARCG_CEF_INCLUDE_PATH}"
         )
         target_link_libraries(CEF::CEF INTERFACE
-            "-Wl,-rpath,${CEF_LIB_PATH} ${CEF_LIB_PATH}/libcef.so"
-            "${CEF_LIB_PATH}/libcef_dll_wrapper.a"
+            "-Wl,-rpath,${CASPARCG_CEF_LIBRARY_PATH}"
+            "${CASPARCG_CEF_LIBRARY_PATH}/libcef.so"
+            "${CASPARCG_CEF_LIBRARY_PATH}/libcef_dll_wrapper.a"
         )
     else()
         casparcg_add_external_project(cef)
diff --git a/modules/html/CMakeLists.txt b/modules/html/CMakeLists.txt
index 052d98c..3e6b70d 100644
--- a/modules/html/CMakeLists.txt
+++ b/modules/html/CMakeLists.txt
@@ -20,5 +20,9 @@ casparcg_add_module_project(html
 )
 target_link_libraries(html PRIVATE ffmpeg CEF::CEF)
-
+
+target_compile_definitions(html PRIVATE
+    "CASPARCG_CEF_RESOURCE_PATH=\"${CASPARCG_CEF_RESOURCE_PATH}\""
+)
+
 target_include_directories(html PRIVATE
     ..
diff --git a/modules/html/html.cpp b/modules/html/html.cpp
index c9993d9..25f793f 100644
--- a/modules/html/html.cpp
+++ b/modules/html/html.cpp
@@ -239,6 +239,10 @@ void init(const core::module_dependencies& dependencies)
         settings.remote_debugging_port        = env::properties().get(L"configuration.html.remote-debugging-port", 0);
         settings.windowless_rendering_enabled = true;
-
+
+        const std::string resource_path(CASPARCG_CEF_RESOURCE_PATH);
+        CefString(&settings.resources_dir_path).FromString(resource_path);
+        CefString(&settings.locales_dir_path).FromString(resource_path + "/locales");
+
         auto cache_path = env::properties().get(L"configuration.html.cache-path", L"cef-cache");
         if (!cache_path.empty()) {
             if (!boost::filesystem::path(cache_path).is_absolute()) {
